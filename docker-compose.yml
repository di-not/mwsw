version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mwsw_mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: mwsw_db
      MYSQL_USER: mwsw_user
      MYSQL_PASSWORD: mwsw_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: unless-stopped

volumes:
  mysql_data: